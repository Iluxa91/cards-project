const initialState = {}

export const authReducer = (state=initialState,action:LoginActionsType) => {
    switch (action.type){
        default:
            return state
    }
}

type LoginActionsType = {
    type:''
}