const initialState = {}

export const profileReducer = (state=initialState,action:LoginActionsType) => {
    switch (action.type){
        default:
            return state
    }
}

type LoginActionsType = {
    type:''
}